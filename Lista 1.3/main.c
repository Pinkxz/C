#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Digite um numero: ");
    scanf("%d", &num);

    num = num - 1;

     printf("O anterior e: %d", num);
    return 0;
}
